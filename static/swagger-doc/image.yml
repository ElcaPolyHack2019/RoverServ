Get snapshot of what the camera sees as jpeg.
---
parameters:
  - name: rover_id
    in: path
    type: string
    description: The id of the rover.
responses:
  404:
    description: The rover with the given id was not found.
  200:
    description: Jpeg image