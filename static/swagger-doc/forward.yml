Move the rover forward for a given time.
---
parameters:
  - name: rover_id
    in: path
    type: string
    description: The id of the rover.
  - name: duration
    in: query string
    type: float
    description: Describes how long the rover moves forward. Default is 1.0.
responses:
  404:
    description: The rover with the given id was not found.
  200:
    description: Ok